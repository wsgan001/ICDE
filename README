1. ACTIVATE TENSORFLOW:
source ~/tensorflow/bin/activate
tensorboard --logdir tf_files/training_summaries &

2. HOW TO RETRAIN:
IMAGE_SIZE=224
ARCHITECTURE="mobilenet_0.50_${IMAGE_SIZE}"

kill: pkill -f "tensorboard"

python -m scripts.retrain \
  --bottleneck_dir=tf_files/bottlenecks \
  --how_many_training_steps=1500 \
  --model_dir=tf_files/models/ \
  --summaries_dir=tf_files/training_summaries/"${ARCHITECTURE}" \
  --output_graph=tf_files/retrained_graph.pb \
  --output_labels=tf_files/retrained_labels.txt \
  --architecture="${ARCHITECTURE}" \
  --image_dir=tf_files/id_photos

3. HOW TO TEST:
python -m scripts.label_image \
    --graph=tf_files/retrained_graph.pb  \
    --image=tf_files/test/name.jpg

4. SEND PATH NAME AS A PARAMETER IN DATA EXTRACTION:
python icde.py --image path_name --type card_type

